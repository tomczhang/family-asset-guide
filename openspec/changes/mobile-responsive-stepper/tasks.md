## 1. 基础设施：断点检测与布局变量

- [x] 1.1 创建 `useIsMobile()` hook（基于 `matchMedia('(max-width: 768px)')` ，监听变化自动更新）
- [x] 1.2 在 `global.css` 中添加移动端媒体查询：隐藏侧边栏右 padding、表单字段单列堆叠、调整内容区间距

## 2. 移动端步骤状态管理

- [x] 2.1 在 `AppContent` 中添加 `currentStep` 状态（0-3），传递给子组件
- [x] 2.2 移动端下根据 `currentStep` 条件渲染：只显示当前步骤对应的编辑器组件

## 3. 底部导航栏

- [x] 3.1 创建 `MobileStepperBar` 组件：包含"上一步"按钮、步骤指示器（N/4）、"下一步"按钮
- [x] 3.2 实现步骤边界逻辑：第一步隐藏"上一步"，最后一步将"下一步"替换为"生成 PDF"
- [x] 3.3 添加固定底部定位样式，并确保主内容区有足够底部 padding 不被遮挡

## 4. 移动端目录面板

- [x] 4.1 创建浮动目录按钮（FAB），定位在底部导航栏上方
- [x] 4.2 创建 `MobileTocOverlay` 组件：全屏覆盖层，列出四个章节名称和计数，当前步骤高亮
- [x] 4.3 实现点击章节跳转：设置 `currentStep` 并关闭面板；点击外部或关闭按钮关闭面板

## 5. Toolbar 移动端适配

- [x] 5.1 移动端下将 Toolbar 操作按钮收缩为汉堡菜单或图标按钮
- [x] 5.2 添加 Toolbar 移动端样式（缩小 logo 字号、调整间距）

## 6. 桌面端验证与整体测试

- [x] 6.1 确认桌面端（>768px）布局和功能完全不受影响
- [x] 6.2 在移动端模拟器中测试完整四步流程（填写、导航、生成 PDF）
- [x] 6.3 测试移动端目录面板的打开、跳转、关闭行为
